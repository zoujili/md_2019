pipeline def and meta in the app's git repo 

gandalf ->  a service managed k8s/ecs  nutshell   turn code into running containers based on metadata in a manifest 
shadowfax 

deploy service :  Gandalf -> CloudFormation + Helm 